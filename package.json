{
  "name": "controle_de_contratos",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "start": "npm --prefix api run start",
    "dev": "npm --prefix api run dev",
    "api:start": "npm --prefix api run start",
    "api:dev": "npm --prefix api run dev",
    "api:install": "npm --prefix api install",
    "postinstall": "npm run api:install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "pg": "^8.16.3"
  }
}
